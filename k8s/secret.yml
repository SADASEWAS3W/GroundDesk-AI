apiVersion: v1
kind: Secret
metadata:
  name: crm-secrets
  namespace: crm
  labels:
    app: crm
    component: config
    part-of: crm
type: Opaque
data:
  # Placeholders — create real secrets at deploy time:
  #   kubectl -n crm create secret generic crm-secrets \
  #     --from-literal=DASHSCOPE_API_KEY="your-key" \
  #     --from-literal=POSTGRES_PASSWORD="secure-password" \
  #     --dry-run=client -o yaml | kubectl apply -f -
  DASHSCOPE_API_KEY: ""
  POSTGRES_PASSWORD: ""
