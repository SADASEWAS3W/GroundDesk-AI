# Bug 修复工作流

1. 复现实际行为，记录预期结果与实际结果。
2. 确认问题所属层，并在修改前检查相邻调用方。
3. 判断风险；涉及 Agent 路由、RAG、数据库、安全或部署时升级为 L3。
4. 新增或修改一个能够复现问题的回归测试。
5. 只实现必要修复，不进行无关重构。
6. 运行受影响测试和任务配置要求的 Harness 门禁。
7. 检查 `git diff`，确认范围、密钥、生成文件和用户既有改动均安全。
8. 记录根因、修复摘要、验证证据和剩余风险。

提交格式：`fix: 简短修复说明`。
