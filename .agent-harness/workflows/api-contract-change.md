# API 契约变更工作流

1. 描述新旧请求/响应契约和兼容性影响。
2. 修改后端 Pydantic 模型及接口行为。
3. 在同一改动中同步前端类型和集中式 API 客户端。
4. 增加后端成功、校验失败和依赖失败测试。
5. 增加前端加载、成功、错误和重试集成测试。
6. 必要时验证任务/审核状态迁移及 Correlation ID。
7. 检查 OpenAPI 输出或 `/docs` 行为。
8. 运行后端、前端、架构、密钥和 Compose 门禁。

破坏性变化必须提供迁移方案并按 L3 审查。
